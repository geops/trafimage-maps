/* eslint-disable no-undef */
// import * as layers from '../../src/config/ch.sbb.netzkarte';
// import MapboxStyleLayer from '../../src/layers/MapboxStyleLayer';
// import TrafimageMapboxLayer from '../../src/layers/TrafimageMapboxLayer';

describe('metadata', () => {
  // beforeEach(() => {
  //   cy.consent();
  //   cy.visit('');
  // });
  // describe('of MapboxStyleLayer in ch.sbb.netzkarte', (done) => {
  //   it(`refers to at least one mapbox layer`, () => {
  //     const mbLayers = Object.values(layers).filter(
  //       (layer) => layer instanceof TrafimageMapboxLayer,
  //     );
  //     console.log(mbLayers);
  //     mbLayers.forEach(async (mbLayer) => {
  //       const styleUrl = `https://map.geops.io/${mbLayer.style}/style.json?key=`;
  //       console.log(styleUrl, process);
  //       const style = fetch(styleUrl).then((response) => response.json());
  //       const layersWithMetadata = object
  //         .values(layers)
  //         .filter(
  //           (l) =>
  //             l.styleLayersFilter &&
  //             l instanceof MapboxStyleLayer &&
  //             l.mapboxLayer === mbLayer,
  //         );
  //       console.log(layersWithMetadata);
  //       layersWithMetadata.forEach((l) => {
  //         expect(style.layers.filter(l.stylElayersFilter)).toBeGreaterThan(0);
  //       });
  //       done();
  //     });
  //   });
  // });
});
